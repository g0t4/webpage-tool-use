# CRUSH.md

## Commands

- Build: None (static HTML/CSS/JS)
- Lint: None (no linter configured)
- Test: None (no tests)

## Code Style

- HTML: Self-closing tags, lowercase attributes
- CSS: BEM naming convention, 2-space indentation
- JavaScript: camelCase, 2-space indentation, semicolons

## Guidelines

- All code should be in a single HTML file
- Use semantic HTML elements
- Keep styles in `<style>` tag
- Inline JavaScript in `<script>` tag
- No external dependencies
- Responsive design using CSS Grid/Flexbox
- Accessible color contrast
- Mobile-first approach
- Keep code readable and maintainable